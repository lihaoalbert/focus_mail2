<%= hidden_field_tag 'org_id', @category.org_id %>
<div class="control-group">
  <%= label_tag 'name', '姓名：', :class => 'control-label' %>
  <div class="controls">
    <%= text_field_tag 'name', @category.name, :class => 'text_field' %>
  </div>
</div>
<div class="form-actions">
  <%= submit_tag '保存', :class => 'btn btn-primary' %>
  <a onclick="$('#categories_form').parent().html('');return false;", class='btn'>Cancel</a>
</div>
